spring:
  application:
    name: spring-ai-mcp-server-leaning
  main:
    web-application-type: none
    banner-mode: off # 关闭 banner 避免stdio传输不相关数据
  ai:
    mcp:
      server:
        stdio: true # 开始 standard input/output 模式
        name: mcp-server-demo # 服务名称 使用时显示的server名称
        version: 0.0.1
logging:
  level:
    root: off # 关闭 日志 避免stdio传输不相关数据
free-api:
  # 大学信息
  url1: https://api.52vmy.cn/api/query/daxue
  # girl image
  url2: https://api.52vmy.cn/api/img/tu/girl
  # girl video
  url3: https://api.kuleu.com/api/MP4_xiaojiejie?type=json
  # 获取新闻
  url4: https://inshorts.deta.dev/news
  # 获取天气
  url5: https://goweather.xyz/weather
  # 根据ip获取详细信息
  url6: https://ipinfo.io